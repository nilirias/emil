program myprogram;

VARS
int : a, b, c, x;

func int myfunc(int: x){
  write(x);
  if (x == 1)
    return(1);
  end
  return(myfunc(x - 1));
}

main()
  a = 5;
  b = (2 + 3) * a - 9 + (a * 7);

  c = myfunc(a);
  write(c);
