program myprogram;

VARS
int : x, y, n, i, o, p;

func void myfunc()
{
  VARS
  int : h, i, j, k, l;
  bool: z;
  char: a;

  h = 1;
  i = 2 + h;
  z = false;
  a = "z";

  if(z == true)
    l = h + 1;
  else
    z = true;
  end
}

func int notmyfunc(int: a)
{
  VARS
  int : v;
}

main()
    x = 1 * 1.1;
    while (x > 1)
      y = 1 * 2.1 ;
    end

    x = 1 * 2;
