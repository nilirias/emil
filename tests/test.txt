program myprogram;

VARS
int : x, y, n, i, o, p;
float: a, b, c;

func void myfunc(int: llll, float: abc, char: xyz, int:aaaaa, int: bbbb)
{
  VARS
  int : h, i, j, k, l;
  bool: z;
  char: a;

  h = 1;
  i = 2 + h;
  z = false;
  a = "z";

  if(z == true)
    l = h + 1;
  else
    z = true;
  end
}

main()
  myfunc(1, 2.0, "a", 5, 2);