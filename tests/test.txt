program myprogram;

VARS
int : sum, num, itr, x;

func int nombredefuncion(int: n){

  if (n == 0 or n == 1)
  return(1);
  end

  return(n * nombredefuncion(n));
}


main()
  sum = 1;
  num = 3;
  itr = 2;

  nombredefuncion(num);